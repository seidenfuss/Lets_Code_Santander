//operador tenario: condição (numero == algumacoisa) ? 'true_saida' : 'false_saida';
// JavaScript possui um operador que permite fazer uma if else inline, isto é, em uma única linha. Operador tenário é representado por ?  : 

let numero = 3;
let paridade = numero % 2 == 0 ? 'par' : 'ímpar';
